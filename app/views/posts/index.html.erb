<h2>All posts</h2>

<%= @posts.each do |post| %>
<% next unless post.user.present? %>
<div>
  <%= link_to image_tag(post.img, :class => 'thumb', :alt => post.content), post %>
</div>
<%= image_tag post.user.img %>
<p><%= post.user.name %></p>
<p><%= post.content %></p>
<% end %>
