<h2>All posts</h2>

<%= @posts.each do |post| %>
<%= link_to image_tag(post.img, :class => 'thumb', :alt => post.content), post %>
<%= image_tag post.user.img if post.user %>
<p><%= post.user.name if post.user %></p>
<p><%= post.content %></p>
<% end %>
